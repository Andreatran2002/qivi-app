// import 'package:flutter/material.dart';
// import 'package:flutter_test/flutter_test.dart';
// import 'package:qivi_app/graphql/__generated__/product.req.gql.dart';

// import 'package:qivi_app/main.dart';
// import 'package:qivi_app/src/app/my_app.dart';
// import 'package:qivi_app/src/client.dart';

// void main() async {
//   var client = await initClient();
//   var request = GProductsReq();
//   client.request(request).first.then((response) {
//     print(response.data);
//     // print(response.data?.test.metadata.runtimeType);
//   });
//   // testWidgets('Counter increments smoke test', (WidgetTester tester) async {
//   //   // Build our app and trigger a frame.
//   //   await tester.pumpWidget(MyApp());

//   //   // Verify that our counter starts at 0.
//   //   expect(find.text('0'), findsOneWidget);
//   //   expect(find.text('1'), findsNothing);

//   //   // Tap the '+' icon and trigger a frame.
//   //   await tester.tap(find.byIcon(Icons.add));
//   //   await tester.pump();

//   //   // Verify that our counter has incremented.
//   //   expect(find.text('0'), findsNothing);
//   //   expect(find.text('1'), findsOneWidget);
//   // });
// }
